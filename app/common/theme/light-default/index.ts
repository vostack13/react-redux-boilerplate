import {ITheme} from 'app/common/theme';
import {colors} from 'app/common/theme/light-default/colors';
import {typography} from 'app/common/theme/light-default/typography';
import {shadow} from 'app/common/theme/light-default/shadow';
import {utils} from 'app/common/theme/light-default/utils';

const theme: ITheme = {
    colors,
    typography,
    shadow,
    utils,
};

export default theme;
