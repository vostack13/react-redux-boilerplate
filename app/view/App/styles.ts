import {css} from '@emotion/core';
import {ITheme} from 'app/view/shared/theme';

const main = css`
    margin-top: 64px;
    padding-top: 32px;
    padding-bottom: 32px;
`;

export default {
    main,
};
