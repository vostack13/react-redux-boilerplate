import {css} from '@emotion/core';

const main = () => css`
    
`;

const form = () => css`
    display: grid;
    grid-row-gap: 16px;
    grid-auto-columns: minmax(280px, 720px);
`;

export default {
    form,
};
