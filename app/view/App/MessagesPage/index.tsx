import React from 'react';

interface IMessagesProps {}

const MessagesPage: React.FC<IMessagesProps> = (props: IMessagesProps) => {

    return <main>
        <h2>Страница сообщений</h2>
    </main>;
};

export default MessagesPage;
